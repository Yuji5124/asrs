# ASRS

AIDA Simple RPG Studio - HTML5ベースの小さなRPG制作ツール。

## ダブルクリックで起動する

```sh
npm run build:single
```

生成された `dist-single/index.html` をダブルクリックすると起動できます。
ルートの `index.html` を直接開いた場合も、生成済みの `dist-single/index.html` に移動します。
